#include<bits/stdc++.h>
using namespace std;

int main()
{
   #ifndef ONLINE_JUDGE
   freopen("input.txt","r",stdin); 
    freopen("q.txt","w",stdout); 
    #endif

   int arr[]{-12,-2,13,434,-65,-5,6};
    int n=7,j=0;
   for(int i=0;i<n;i++)
   {
     if(arr[i]<0)
     {
      if(i!=j){
        swap(arr[i],arr[j]);
      }
      j++;
     }
   }
   

   for(int i=0;i<n;i++)
   {
    cout<<" "<<arr[i];
   }
   return 0;
}
